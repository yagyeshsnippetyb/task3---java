# task3---java This project is a Java console-based Student Result Processing System that takes marks as input and calculates percentage and grades.  
It supports multiple student entries using loops and validates marks within the allowed range.  
Grades are assigned using if-else and switch-case statements.  
The application displays a final result summary including pass/fail status.  
Compatible with IntelliJ IDEA, Eclipse, and VS Code.
